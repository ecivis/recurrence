/**
* See https://coldbox.ortusbooks.com/hmvc/modules/moduleconfig for info
*/
component {

    this.modelNamespace = "recurrence";
    this.cfMapping = "recurrence";
    this.autoMapModules = true;

    /**
    * There isn't anything special required for configuration
    */
    public void function configure() {
        return;
    }

}